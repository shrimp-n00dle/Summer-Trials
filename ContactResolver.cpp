#include "ContactResolver.h"


void ContactResolver::resolveContacts(std::vector<P6::ParticleContact*> contacts, float time)
{

	//you can loop through a std vector class

	/*for (unsigned i = 0; i < conacts.size(); i++)
	{
	*/
}